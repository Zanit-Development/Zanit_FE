/**
 * 인풋 인터페이스, 타입 옵션
 */

import { INPUT_SIZE_VARIANTS, INPUT_TYPE_VARIANTS } from "../../../libs/interface/typeCommon";

export interface StyledInputOption {
  typevariants: INPUT_TYPE_VARIANTS;
  sizevariants: INPUT_SIZE_VARIANTS;
}
