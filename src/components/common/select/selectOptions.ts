import { SELECT_SIZE_VARIANTS, SELECT_TYPE_VARIANTS } from "../../../libs/interface/typeCommon";

export interface StyledSelectOption {
  typevariants: SELECT_TYPE_VARIANTS;
  sizevarignts: SELECT_SIZE_VARIANTS;
}
